# Environment Configuration Example
# Copy this file to .env and configure for your environment

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
ENVIRONMENT_PROPOGATION_VALUE=ENVIRONMENT_FILE_EXAMPLE

# Environment setting (development, staging, production)
ENVIRONMENT=development

# =============================================================================
# SHARED DATABASE CREDENTIALS (used by both app and Dagster)
# =============================================================================
# # DB type to use (postgres, mysql, or sqlite)
# DB_TYPE=postgres

# # Shared credentials (for postgres/mysql)
# DB_USERNAME=postgres  # e.g., postgres or your_db_user
# DB_PASSWORD=password
# DB_HOST=localhost
# DB_PORT=5432  # or 3306 for mysql
# DB_NAME=postgres  # e.g., dagster or myapp_dev

# # For SQLite only (overrides above if DB_TYPE=sqlite)
# SQLITE_BASE_DIR=.data/db  # e.g., .data/db

# =============================================================================
# DATABASE CONNECTION POOL SETTINGS
# =============================================================================
# # Fine-tune connection pooling for your workload
# DATABASE_POOL_SIZE=5
# DATABASE_MAX_OVERFLOW=10
# DATABASE_POOL_TIMEOUT=30
# DATABASE_POOL_RECYCLE=3600

# # =============================================================================
# # DATABASE DEBUGGING
# # =============================================================================
# # Enable SQL query logging (useful for development, disable in production)
# SQL_ECHO=false

# =============================================================================
# DAGSTER SETTINGS
# =============================================================================
# # Enable Logging to Database
DAGSTER_DATABASE_URL=postgresql://postgres:password@localhost:5432/system_dagster

